[
  {
    "id": "q1",
    "question": "What port does the default HTTP server listen on?",
    "contexts": [
      "The application server binds to port 3000 by default when no PORT environment variable is set.",
      "Static assets are served from the /public directory."
    ],
    "answer": "By default, the HTTP server listens on port 3000.",
    "groundTruth": "The default HTTP server listens on port 3000 unless the PORT environment variable overrides it."
  },
  {
    "id": "q2",
    "question": "How does the retriever rank documents?",
    "contexts": [
      "Documents are chunked into 500-token windows with 50-token overlap before embedding.",
      "The retriever combines BM25 lexical scores with cosine similarity from dense embeddings using a weighted sum, then reranks the top 20 candidates with a cross-encoder."
    ],
    "answer": "The retriever ranks documents using a hybrid approach: it combines BM25 keyword scores with dense embedding similarity, then reranks the top candidates with a cross-encoder model.",
    "groundTruth": "Retrieval combines BM25 and dense embedding similarity, and the top candidates are reranked with a cross-encoder."
  },
  {
    "id": "q3",
    "question": "What database does the project use for vector storage?",
    "contexts": [
      "User authentication tokens are stored in Redis with a 24 hour TTL.",
      "Application logs are shipped to an S3 bucket for long-term retention."
    ],
    "answer": "The project stores vectors in Pinecone, a managed vector database service.",
    "groundTruth": "The provided context does not specify the vector database used."
  }
]
